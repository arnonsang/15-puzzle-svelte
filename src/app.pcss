/* Write your global styles here, in PostCSS syntax */
@tailwind base;
@tailwind components;
@tailwind utilities;

@font-face {
    font-family: Ice cream;
    src: url("./lib/assets/fonts/Ice cream.ttf") format("truetype");
    font-style: normal;
    font-weight: 800;
    font-display: swap;
  }
  
  @layer base {
    html {
      font-family: Ice cream, sans-serif;
    }
  }